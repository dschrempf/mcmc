name:                mcmc
version:             0.1.0.0
github:              "dschrempf/mcmc"
license:             BSD3
author:              "Dominik Schrempf"
maintainer:          "dominik.schrempf@gmail.com"
copyright:           "Dominik Schrempf (2020)"

extra-source-files:
- README.md
- ChangeLog.md

synopsis:            Sample from a posterior using Markov chain Monte Carlo
category:            Math, Statistics
description:         Please see the README on GitHub at <https://github.com/dschrempf/mcmc#readme>

library:
  source-dirs: src
  dependencies:
    - base >= 4.7 && < 5
    - log-domain
    - microlens
    - mwc-random
    - statistics
    - transformers
  exposed-modules:
    - Statistics.Mcmc.Metropolis
    - Statistics.Mcmc.Moves
    - Statistics.Mcmc.Tools
    - Statistics.Mcmc.Types

benchmarks:
  mcmc-bench:
    main:        Bench.hs
    source-dirs: bench
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - base >= 4.7 && < 5
      - criterion
      - log-domain
      - mcmc
      - microlens
      - mwc-random
      - statistics
      - vector

tests:
  mcmc-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - base >= 4.7 && < 5
      - hspec
      - hspec-discover
      - mcmc
