resolver: nightly-2021-09-17

packages:
  - ./mcmc
  - ./mcmc-examples
  - ./mcmc-statistics

extra-deps:
  # - circular-0.4.0.0
  # - dirichlet-0.1.0.4
  - ../circular
  - ../dirichlet
  - ../covariance

# # LLVM.
# ghc-options: {"$everything": -O2 -fllvm}
